import { VoiceActorPipe } from './voice-actor.pipe';

describe('VoiceActorPipe', () => {
  it('create an instance', () => {
    const pipe = new VoiceActorPipe();
    expect(pipe).toBeTruthy();
  });
});
